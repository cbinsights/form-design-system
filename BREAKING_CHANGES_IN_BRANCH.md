# Breaking changes introduced in this branch
so i can keep track

- `font`/`size` properties removed/changed in dictionary
    - affects both CSS and JS distributions
- `typemod`/`typeset` classes in base-styles
    - `typeset--<thing>` -> `type--<typeTypeLOL>`
