import { Meta, Story } from "@storybook/addon-docs/blocks";
import { generalStyles } from 'components/util/storybook';
import { ClassTableLayout } from "components/util/Table"
import FDS from 'lib/dictionary/js/styleConstants';
import { Typesetting, InvertedTypesetting, Headers } from './styleutils';

export const positionStyles = {
  ...generalStyles,
  border: `1px solid ${FDS.COLOR_CHARCOAL}`,
  padding: 2 
}

export const boxStyles = {
  height: 30,
  width: 30,
  background: FDS.COLOR_CHARCOAL,
  borderRadius: 3
}

<Meta title="Style Utilities/Typesetting"  />

# Typesetting

<ClassTableLayout
  headers={['class', 'description']}
  rows={[
    ['type--head1', 'Top level heading'],
    ['type--head2', 'Secondary heading'],
    ['type--head3', 'Tertiary heading'],
    ['type--head4', 'Minor heading'],
    ['type--big', 'Big body text'],
    ['type--data', 'Adds standard margin to all sides'],
    ['type--caption', 'Adds standard margin to all sides'],
    ['type--link', 'Adds standard margin to all sides'],
  ]} 
/>

To set the display of a given heading tag, use one of the typesetting classes below.
(By default, HTML heading tags are set to match the default body text.)

<Typesetting />

## Inverted
Select Typesetting classes can get an optimized display for
dark backgrounds by being placed inside an `.inverted` context:

<InvertedTypesetting />