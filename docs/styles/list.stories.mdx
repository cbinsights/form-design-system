import { Meta, Story, Preview, Props, Source } from "@storybook/addon-docs/blocks";
import { withKnobs, text, boolean } from '@storybook/addon-knobs';
import { arrayToOptions, StoryWrapper, StoryItem, Item, generalStyles, containerStyles, layoutStyle, labelStyle } from 'components/util/storybook';
import { ClassTableLayout } from "components/util/Table"
import FDS from 'lib/dictionary/js/styleConstants';

export const positionStyles = {
  ...generalStyles,
  border: `1px solid ${FDS.COLOR_CHARCOAL}`,
  padding: 1 
}

<Meta title="Style Utilities/List"  />

## Decorated Lists

<Story name="Decorated Lists">
  <>
    <ClassTableLayout
      headers={['class', 'description']}
      rows={[
        ['list--decorated', 'Enables standard list decoration'],
      ]} 
    />
    <div>With <span style={labelStyle}>list--decorated</span>:</div>
    <hr />
    <ul className="list--decorated">
      <li>Lorem</li>
      <li>Ipsum</li>
    </ul>
    <br />
    <div>Without (by default, lists have their styles reset):</div>
    <hr />
    <ul>
      <li>Lorem</li>
      <li>Ipsum</li>
    </ul>
  </>
</Story>
