import { useRef } from 'react';
import { Meta, Story } from "@storybook/addon-docs/blocks";
import { TableLayout } from 'util/storybook-docs/Table';
import FDS from 'lib/dictionary/js/styleConstants';
import cc from 'classcat';
import Demo from './Demo';

<Meta title="hooks/useScrollAttributes" />

# useScrollAttributes

Returns if element is scrolled / not scrolled to left and right.

```
// Usage
const ref = useRef();
const [isAtScrollStart, isAtScrollEnd] = useScrollAttributes(ref); 
```

<Story name="primary">
  <Demo />
</Story>