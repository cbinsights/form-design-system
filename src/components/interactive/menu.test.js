import React from 'react';
// import { shallow } from 'enzyme';

// import Menu from './Menu';

export const MOCK_ITEMS = ['one', 'two', 'three', 'four', 'five', 'six', 'seven', 'eight']
  .map((label) => <div className="color--red">{label}</div>);
