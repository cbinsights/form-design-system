import { Meta, Story, Props } from "@storybook/addon-docs/blocks";

import Menu from "./Menu";
import { MOCK_ITEMS } from './menu.test';

<Meta title="Interactive|Menu" component={Menu} />

# Menu

<Story name="Test">
  <div className="alignChild--center--center" style={{ height: '100%', width: '100%' }}>
    <Menu
      items={MOCK_ITEMS}
      trigger={<button className="padding--all--half bgColor--red inverted">JSX lol</button>}
      onChange={(wat) => {console.dir(wat)}}
    />
  </div>
</Story>

<Props of={Menu} />
