import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, text, optionsKnob as options } from '@storybook/addon-knobs';
import { arrayToOptions, ImportPath } from 'components/util/storybook';

import Badge, { SIZES } from '.';

<Meta title="Components/Badge" decorators={[withKnobs]} component={Badge} />

# Badge

<ImportPath component={Badge}  />

<Story name="Knobs" parameters={{ docs: { disable: true }}}>
  <Badge 
    label={text('label', 'Badge')} 
    size={options('size', arrayToOptions(SIZES), undefined, {
      display: 'inline-radio',
    })}
  />
</Story>

<Preview isExpanded>
  <Badge 
    label="Badge"
  />
</Preview>

<Props of={Badge} />

### Sizes

<Preview>
  <Story name="Sizes">
    {SIZES.map(size => (
      <div className="margin--right--half display--inlineBlock">
        <Badge size={size} label="Badge" />
      </div>
    ))}
  </Story>
</Preview>
