import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { useRef, useState } from 'react';
import Info from 'util/storybook-docs/Info';
import ImportPath from 'util/storybook-docs/ImportPath';
import { arrayToOptions } from 'util/storybook-docs/knobHelpers';

import { Popover, Button, Dialog, Menu, MenuItem } from 'components';

<Meta title="Examples/Alignment" />

## Inline buttons

<Preview>
  <Story name="Inline buttons">
    {() => (
      <div>
        Using the `.resetButton` class name you can inline a button to launch interactions
        like:{' '}
        <button
          className="resetButton type--link"
          onClick={() => window.alert('Terms of service.')}
        >
          see terms
        </button>
        .
      </div>
    )}
  </Story>
</Preview>
