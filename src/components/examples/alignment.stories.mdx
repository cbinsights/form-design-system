import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { useRef, useState } from 'react'
import {
  withKnobs,
  optionsKnob as options,
  select,
  number,
  boolean,
} from '@storybook/addon-knobs';
import { arrayToOptions, Info, ImportPath } from 'components/util/storybook';

import Popover from 'components/Popover';
import Button from 'components/Button';
import Dialog from 'components/Dialog';
import Menu from 'components/Menu';
import MenuItem from 'components/Menu/MenuItem';

<Meta title="Examples/Alignment" />

## Inline buttons

<Preview>
  <Story name="Inline buttons">
    {() => (
      <div>
        Using the `.resetButton` class name you can inline a button to launch interactions like:{' '}
        <button className="resetButton type--link" onClick={() => window.alert('Terms of service.')}>
          see terms
        </button>.
      </div>
    )}
  </Story>
</Preview>
