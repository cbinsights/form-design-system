import { Meta, Preview, Story, Props } from "@storybook/addon-docs/blocks";
import {
  withKnobs,
  text,
  boolean,
  optionsKnob as options
} from "@storybook/addon-knobs";
import { ImportPath, State } from 'components/util/storybook';
import DropdownButton from ".";

<Meta title="Components/DropdownButton" decorators={[withKnobs]} component={DropdownButton} />

# DropdownButton

<ImportPath component={DropdownButton} section="interactive" />

<Story name="Knobs" parameters={{ docs: { disable: true }}}>
  <DropdownButton
    isFullWidth={boolean("isFullWidth", undefined)}
    isActive={boolean('isActive', undefined)}
  >
    {text("children", 'DropdownButton')}
  </DropdownButton>
</Story>

<Preview isExpanded>
  <DropdownButton>Dropdown Button</DropdownButton>
</Preview>

<Props of={DropdownButton} />

### isActive
**Click to toggle on and off**.

<Preview>
  <Story name="isActive">
    <State
      initialValue={true}
      render={(value, setValue) => (
        <DropdownButton onClick={() => setValue(!value)} isActive={value}>
          Hello world
        </DropdownButton>
      )}
    />
  </Story>
</Preview>

DropDownButton will render any JSX passed to it

<Preview>
  <Story name="JSX">
    <DropdownButton>
      <span className="fontWeight--bold fontSize--default">Hello</span> world
    </DropdownButton>
  </Story>
</Preview>

