import { Meta } from '@storybook/addon-docs/blocks';
import { TableLayout } from 'util/storybook-docs/Table';
import { action } from '@storybook/addon-actions';

<Meta title="Style Utilities/Misc" />

export const Pointer = () => (
  <p className="pointer-events-none" onClick={action('click')}>
    Clicking on this text will not fire the onClick event attached to it
  </p>
);

# Misc

<TableLayout
  isCSS
  headers={['class', 'description', 'sample']}
  rows={[['pointer-events-none', 'Disables pointer events', { children: <Pointer /> }]]}
/>
