import { Meta, Story } from '@storybook/addon-docs/blocks';
import {
  Example,
  generalStyles,
  containerStyles,
  labelStyle,
} from 'base-styles/styledocUtils';
import FDS from 'lib/dictionary/js/styleConstants';
import { TableLayout } from 'util/storybook-docs/Table';

<Meta title="Style Utilities/Background" />

## Background Colors

Sets background color of an element. Signature: `bgColor--<color>`

<Story name="Background Colors">
  <>
    <TableLayout
      isCSS
      headers={['class', 'description', 'sample']}
      rows={[
        ['bgColor--white', 'Sets background color `var(--color-white)`'],
        ['bgColor--haze', 'Sets background color `var(--color-haze)`'],
        ['bgColor--lightGray', 'Sets background color `var(--color-lightGray)`'],
        ['bgColor--orange', 'Sets background color `var(--color-orange)`'],
        ['bgColor--charcoal', 'Sets background color `var(--color-charcoal)`'],
        ['bgColor--navy', 'Sets background color `var(--color-navy)`'],
        ['bgColor--blue', 'Sets background color `var(--color-blue)`'],
        ['bgColor--aqua', 'Sets background color `var(--color-aqua)`'],
        ['bgColor--gray', 'Sets background color `var(--color-gray)`'],
        ['bgColor--red', 'Sets background color `var(--color-red)`'],
        ['bgColor--purple', 'Sets background color `var(--color-purple)`'],
      ].map((row) => [row[0], row[1], { className: row[0] }])}
    />
  </>
</Story>

## Fade gradients

Sets background of an element to a linear gradient simulating a fade effect.

<Story name="Fade gradients">
  <>
    <TableLayout
      isCSS
      headers={['class', 'description', 'sample']}
      rows={[
        [
          'fade--left--white',
          'Sets linear gradient from left (white) to right (transparent)',
        ],
        [
          'fade--top--white',
          'Sets linear gradient from top (white) to bottom (transparent)',
        ],
        [
          'fade--bottom--white',
          'Sets linear gradient from bottom (white) to top (transparent)',
        ],
        [
          'fade--right--white',
          'Sets linear gradient from right (white) to left (transparent)',
        ],
        [
          'fade--left--haze',
          'Sets linear gradient from left (haze) to right (transparent)',
        ],
        [
          'fade--top--haze',
          'Sets linear gradient from top (haze) to bottom (transparent)',
        ],
        [
          'fade--bottom--haze',
          'Sets linear gradient from bottom (haze) to top (transparent)',
        ],
        [
          'fade--right--haze',
          'Sets linear gradient from right (haze) to left (transparent)',
        ],
      ].map((row) => [
        row[0],
        row[1],
        {
          className: 'transparentpattern',
          style: {
            display: 'inline-flex',
            boxSizing: 'border-box',
            width: '100%',
            padding: 0,
          },
          children: <div className={row[0]} style={{ width: '100%', height: '52px' }} />,
        },
      ])}
    />
  </>
</Story>
