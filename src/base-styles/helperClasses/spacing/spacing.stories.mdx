import { Meta, Story } from '@storybook/addon-docs/blocks';
import { TableLayout } from 'util/storybook-docs/Table';
import FDS from 'lib/dictionary/js/styleConstants';

export const borderStyle = {
  border: `1px solid ${FDS.COLOR_SAND}`,
};

<Meta title="Style Utilities/Spacing" />

# Spacing

export const Scale = ({ label, num }) => (
  <div
    style={{
      display: 'inline-flex',
      flexDirection: 'column',
      alignItems: 'center',
      minWidth: '20px',
      marginRight: 4,
      marginTop: -4,
    }}
  >
    <span style={{ fontWeight: 'bold' }}>{label}</span>
    <div
      style={{ display: 'inline-flex', flexDirection: 'column', alignItems: 'center' }}
    >
      <div
        style={{
          height: '16px',
          background: FDS.COLOR_AQUA,
          marginRight: 4,
          marginLeft: 4,
          marginBottom: 2,
          width: `${num}px`,
        }}
      />
      <small
        style={{ fontSize: 9, lineHeight: '14px', fontStyle: 'italic' }}
        className="color--slate"
      >
        {num}px
      </small>
    </div>
  </div>
);

export const SpacingExample = ({ className }) => (
  <div
    className="display--inlineBlock margin--right--s margin--bottom--s"
    style={{ border: className.startsWith('padding') ? '1px dashed #333' : '' }}
  >
    <div
      className={`${
        className.startsWith('padding') && className
      } display--inlineBlock border--all `}
      style={{
        backgroundColor: className.startsWith('padding') ? FDS.COLOR_MINT : '#F6BDC1',
      }}
    >
      <div
        className={`${
          className.startsWith('margin') && className
        } display--inlineBlock rounded--all`}
      >
        <div
          className={`padding--all--s color--charcoal fontWeight--bold bgColor--white elevation--1 rounded--all`}
          style={{ border: className.startsWith('margin') ? `1px dashed #333` : '' }}
        >
          {className}
        </div>
      </div>
    </div>
  </div>
);

<Story name="Margin and Padding">
  <>
    <TableLayout
      rows={[
        ['**Type**', '`margin` | `padding`'],
        ['**Direction**', '`left`  |  `right` | `top` | `bottom` | `x` | `y` | `all`'],
        [
          '**Scale (default: m)**',
          {
            children: (
              <>
                <Scale label="xs" num={4} />
                <Scale label="s" num={8} />
                <Scale label="m" num={16} />
                <Scale label="l" num={24} />
                <Scale label="xl" num={32} />
              </>
            ),
          },
        ],
      ]}
    />
    <SpacingExample className="margin--top" />
    <SpacingExample className="margin--bottom--s" />
    <SpacingExample className="margin--x--xl" />
    <SpacingExample className="margin--y--xs" />
    <SpacingExample className="margin--left--m" />
    <SpacingExample className="padding--y--xs" />
    <SpacingExample className="padding--x" />
    <SpacingExample className="padding--all--xs" />
    <SpacingExample className="padding--left--l" />
  </>
</Story>
