import { Meta, Story } from "@storybook/addon-docs/blocks";
import { Example, generalStyles, containerStyles, labelStyle } from 'base-styles/styledocUtils';
import { TableLayout } from 'util/storybook-docs/Table';
import FDS from 'lib/dictionary/js/styleConstants';

<Meta title="Style Utilities/Layout"  />

export const borderHelperStyles = {
  ...generalStyles,
  backgroundColor: '#333',
  margin: ".5rem"
}

## Media Sizing
For standard media like avatars and icons,
these classes set square dimenions on the selected
element to match standard media sizes.

<Story name="Media Sizing">
  <>
    <TableLayout
      isCSS
      headers={['class', 'description']}
      rows={[
        ['media--xs', `Sets height & width to \`${FDS.MEDIA_XS}\` (\`var(--media-xs)\`)`],
        ['media--s', `Sets height & width to \`${FDS.MEDIA_S}\` (\`var(--media-s)\`)`],
        ['media--m', `Sets height & width to \`${FDS.MEDIA_M}\` (\`var(--media-m)\`)`],
        ['media--l', `Sets height & width to \`${FDS.MEDIA_L}\` (\`var(--media-l)\`)`],
        ['media--xl', `Sets height & width to \`${FDS.MEDIA_XL}\` (\`var(--media-xl)\`)`],
      ]} 
    />
    <div style={containerStyles}>
      <Example label="media--xs"><div className="media--xs rounded--all" style={borderHelperStyles} /></Example>
      <Example label="media--s"><div className="media--s rounded--all" style={borderHelperStyles} /></Example>
      <Example label="media--m"><div className="media--m rounded--all" style={borderHelperStyles} /></Example>
      <Example label="media--l"><div className="media--l rounded--all" style={borderHelperStyles} /></Example>
      <Example label="media--xl"><div className="media--xl rounded--all" style={borderHelperStyles} /></Example>
    </div>
  </>
</Story>

export const widthStyles = { height: 50 }

## Width
Sets element width in percentage.

<Story name="Width">
  <>
    <TableLayout
      isCSS
      headers={['class', 'description']}
      rows={[
        ['span--100', 'Sets `width` to `100%`'],
        ['span--50', 'Sets `width` to `50%`'],
      ]} 
    />
    <span style={labelStyle}>span--100</span>
    <div className="span--100 rounded--all bgColor--charcoal margin--top--half" style={widthStyles} />
    <br />
    <span style={labelStyle}>span--50</span>
    <div className="span--50 rounded--all bgColor--charcoal margin--top--half" style={widthStyles} />
  </>
</Story>

## Display

<Story name="Display">
  <TableLayout
    isCSS
    headers={['class', 'description']}
    rows={[
      ['display--none', 'Sets `display` to `none`'],
      ['display--block', 'Sets `display` to `block`'],
      ['display--flex', 'Sets `display` to `flex`'],
      ['display--inline', 'Sets `display` to `inline`'],
      ['display--inlineBlock', 'Sets `display` to `inline-block`'],
      ['display--inlineFlex', 'Sets `display` to `inline-flex`'],
      ['display--grid', 'Sets `display` to `grid`'],
    ]} 
  />
</Story>