<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>fds-dictionary</title>
    <link rel="stylesheet" href="assets/pure-min.css" type="text/css">
    <link rel="stylesheet" href="assets/custom.css" type="text/css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.css" type="text/css">
  </head>
  <body>
    <section class="bounds">
      <p><a href="https://github.com/cbinsights/form-design-system">&larr; github</a></p>
      <h1><code>fds-dictionary</code></h1>
      <div style="font-size: 18px;">
        <p><strong>For usage in CSS:</strong> all of the custom properties below should already be globally available in your app.</p>
        <p><strong>For usage in JS:</strong> <code>import FDS from 'fds-dictionary/dist/js/styleConstants.js'; /* usage: FDS.COLOR_MINT */</code></p>
      </div>
    </section>


    <section class="bounds">
      <h2>Color</h2>
      <table class="propertiesTable">
        <thead>
          <tr>
            <th style="padding-left: 22px;">🎨</th>
            <th id="css">CSS var</th>
            <th id="js">JS var</th>
            <th id="hex">hex</th>
            <th id="rgba">rgba</th>
          </tr>
        </thead>
        <tbody>
          {{#each this.color}}
            <tr>
              <th><div class="swatch" style="background-color: {{this.values.rgba}}"></div></th>
              <td class="js-clipboard" data-clipboard-text="{{this.varNames.css}}" headers="css">{{this.varNames.css}}</td>
              <td class="js-clipboard" data-clipboard-text="{{this.varNames.js}}" headers="js">{{this.varNames.js}}</td>
              <td class="js-clipboard" data-clipboard-text="#{{this.values.hex}}" headers="hex">#{{this.values.hex}}</td>
              <td class="js-clipboard" data-clipboard-text="{{this.values.rgba}}" headers="rgba">{{this.values.rgba}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </section>

    <section class="bounds">
      <h2>Layout</h2>
      <table class="propertiesTable">
        <thead>
          <tr>
            <th id="css">CSS var</th>
            <th id="js">JS var</th>
            <th id="value">value</th>
          </tr>
        </thead>
        <tbody>
          {{#each this.layout}}
            <tr>
              <td class="js-clipboard" data-clipboard-text="{{this.varNames.css}}" headers="css">{{this.varNames.css}}</td>
              <td class="js-clipboard" data-clipboard-text="{{this.varNames.js}}" headers="js">{{this.varNames.js}}</td>
              <td class="js-clipboard" data-clipboard-text="{{this.value}}" headers="value">{{this.value}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </section>

    <section class="bounds">
      <h2>Font</h1>
      <table class="propertiesTable">
        <thead>
          <tr>
            <th style="min-width: 360px;" id="css">CSS var</th>
            <th id="js">JS var</th>
            <th id="value">value</th>
          </tr>
        </thead>
        <tbody>
          {{#each this.font}}
            <tr>
              <td class="js-clipboard" data-clipboard-text="{{this.varNames.css}}" headers="css">{{this.varNames.css}}</td>
              <td class="js-clipboard" data-clipboard-text="{{this.varNames.js}}" headers="js">{{this.varNames.js}}</td>
              <td class="js-clipboard" data-clipboard-text="{{this.value}}" headers="value">{{this.value}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </section>

    <section class="bounds">
      <h2>Material UI Palette</h2>

      <p>Palette objects following <a href="https://material.io/design/color/the-color-system.html#">material design convention of light and dark variants</a>.</p>
      <p><strong>Import statement example:</strong><br /><code>import { timberwolf, sienna, blue, violet } from 'fds-dictionary/dist/js/materialPalette.js';</code></p>

      <div class="palettes">
        {{#each this.color}}
          <div class="palette">
            <p class="palette-name"><code>{{this.varNames.human}}</code></p>
            <ul class="palette-swatches">
              {{#each this.materialPalette}}
                <li>
                  <div
                    class="swatch swatch--material {{#if this.isBase}}swatch--base{{/if}} {{#if this.isAccent}}swatch--accent{{/if}}"
                    style="background-color: {{this.value}};"
                  ></div>
                  <div class="swatch-key">{{this.key}}</div>
                </li>
              {{/each}}
            </ul>
          </div>
        {{/each}}
      </div>
    </section>

  <script
    src="https://code.jquery.com/jquery-1.12.4.min.js"
    integrity="sha256-ZosEbRLbNQzLpnKIkEdrPv7lOy9C27hHQ+Xp8a4MxAQ="
    crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
  <script src="https://unpkg.com/clipboard@2.0.0/dist/clipboard.min.js"></script>
  <script type="text/javascript">
    function autorun() {
      toastr.options.preventDuplicates = true;

      var clipboard = new ClipboardJS('.js-clipboard');
      var copyIndicator = $('#copyIndicator');

      clipboard.on('success', function(e) {
        toastr.clear();
        toastr.info(`Copied <strong style='color: #003366'>"${e.text}"</strong>`);
      });
      clipboard.on('error', function(e) {
        toastr.clear();
        toastr.error('Failed to copy to clipboard 😓')
      });

    }
    if (document.addEventListener) document.addEventListener("DOMContentLoaded", autorun, false);
    else if (document.attachEvent) document.attachEvent("onreadystatechange", autorun);
    else window.onload = autorun;
  </script>
  </body>
</html>
